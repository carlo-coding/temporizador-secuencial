import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#4F46E5",
    secondary: "#10B981",
    surface: "#FFFFFF",
    background: "#F5F6FA",
  },
};

export default theme;
